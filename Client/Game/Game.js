class Game
{
    constructor()
    {
    }

    onResize(size)
    {
    }

    draw()
    {
        textSize(100);
        fill(1);
        textAlign(CENTER, CENTER);
        text("Game started!", width / 2, height / 2);
    }
}